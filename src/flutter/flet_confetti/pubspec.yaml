name: flet_confetti
description: Flet Confetti control
version: 0.2.0
publish_to: none

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  collection: ^1.16.0
  confetti: ^0.7.0

  flet:
    git:
      url: https://github.com/flet-dev/flet.git
      path: packages/flet
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
